// Socketed main shaft with a thin removable sacrificial base.
// Render with:
//   openscad -o main_with_removable_base.stl main_with_removable_base.scad

use <main.scad>

main_shaft(with_base = true);
